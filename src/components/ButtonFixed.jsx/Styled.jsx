import styled from 'styled-components';

export const ButtonContainer = styled.div`
    max-width: 60px;
    position: fixed;
    bottom: 40px;
    right: 40px;
    [src]{
        width: 100%;
    }
`