import styled from 'styled-components';

export const ButtonContainer = styled.div`
    max-width: 60px;
    position: fixed;
    bottom: 50px;
    right: 50px;
    [src]{
        width: 100%;
    }
`